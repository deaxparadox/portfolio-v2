import { RoutePlaceholder } from "@/components/routes/route-placeholder";

export default function ProjectsPage() {
  return (
    <RoutePlaceholder
      title="Projects"
      description="Placeholder — project listing content arrives in a later milestone."
    />
  );
}
