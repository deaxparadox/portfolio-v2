import { RoutePlaceholder } from "@/components/routes/route-placeholder";

export default function ContactPage() {
  return (
    <RoutePlaceholder
      title="Contact"
      description="Placeholder — contact content arrives in a later milestone."
    />
  );
}
