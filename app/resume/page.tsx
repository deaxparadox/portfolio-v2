import { RoutePlaceholder } from "@/components/routes/route-placeholder";

export default function ResumePage() {
  return (
    <RoutePlaceholder
      title="Resume"
      description="Placeholder — ATS/human-friendly resume downloads arrive in a later milestone."
    />
  );
}
