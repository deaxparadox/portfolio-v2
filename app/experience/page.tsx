import { RoutePlaceholder } from "@/components/routes/route-placeholder";

export default function ExperiencePage() {
  return (
    <RoutePlaceholder
      title="Experience"
      description="Placeholder — experience timeline content arrives in a later milestone."
    />
  );
}
